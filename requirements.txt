# Flask 2.2.5 – CVE-2023-30861 (set-cookie samesite bypass, fixed in 2.3.2)
Flask==2.3.3
Flask-SQLAlchemy==2.5.1
# Werkzeug 2.2.2 – CVE-2023-25577, CVE-2023-23934 (fixed in 2.2.3); Python 3.11 compat (1.x uses removed collections ABCs)
Werkzeug==2.3.8
# Jinja2 3.1.1 – CVE-2024-22195 (XSS via autoescape, fixed in 3.1.2); Python 3.11 compat (2.x uses removed collections ABCs)
Jinja2==3.1.3
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
pytest==7.4.3

# YAML configuration parsing
PyYAML==6.0.1

# Image processing for scan evidence attachments
# Pillow 9.5.0 – CVE-2023-44271 (uncontrolled resource consumption, fixed in 10.0.0); minimum version with Python 3.11 wheels
Pillow==10.3.0

# HTTP client for NVD/CVE database API calls and webhook notifications
requests==2.32.0

# SSH client for remote scanner agent management
paramiko==3.4.0
# cryptography 38.0.4 – minimum patch release with Python 3.11 wheels (38.0.1-38.0.2 have no cp311 wheel)
cryptography==38.0.4

# Database
SQLAlchemy==1.4.46
psycopg2-binary==2.9.9

# Caching layer
redis==4.3.4

# WSGI server
gunicorn==20.1.0